// Include section
#include <stdio.h>

// Main function in the program, no program arguments supported
int main(void) {

    // Print a string to the console
    printf("%s", "hello world\n");

    // Return 0 to showcase that the program has ended cuccessfully
    return 0;
}
